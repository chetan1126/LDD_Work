sudo insmod platform_char.ko          # Load module
dmesg | tail -10                      # Check logs
ls /dev/pchar0                        # Verify device created
echo "hello" | sudo tee /dev/pchar0   # Write to device
sudo cat /dev/pchar0                  # Read back
sudo rmmod platform_char              # Remove module